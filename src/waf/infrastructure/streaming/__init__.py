"""streaming — 메시지 파이프(Kafka 등) 어댑터.

도메인은 이 패키지를 모른다(인프라 전용). 상위 계층은 MessageChannel 포트에만 의존하고,
실제 전송 수단(Kafka / 인메모리)은 어댑터로 갈아끼운다 → '데이터를 파이프에 넣으면 학습·
테스트·추론이 돈다'는 단순한 모델을 브로커 유무와 무관하게 동일 코드로 다룬다.
"""
